# PORT: Port the server listens on. Defaults to 3000 when unset.
PORT="3000"

# APP_URL: Public base URL where this app is hosted.
# Used to build shareable collection links and QR codes.
APP_URL="http://localhost:3000"
